# Azure Bot registration credentials. Copy to .env and fill in.
# teams.py reads these directly from the environment — there is no config object.
CLIENT_ID=
CLIENT_SECRET=
TENANT_ID=

# Defaults to 3978. The middleware sample already uses that, so this one uses 3979
# to let both run side by side.
PORT=3979
